// Dummy function that returns a greeting
function getGreeting(name) {
  return `Hello, ${name}!`;
}

// Dummy function that adds two numbers
function add(a, b) {
  return a + b;
}

// Example usage
console.log(getGreeting('World'));
console.log('2 + 3 =', add(2, 3));
